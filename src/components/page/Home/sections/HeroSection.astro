---
import AnimateTitle from "@/components/page/Home/compose/hero/AnimateTitle.astro";
import { Button } from "@/components/ui/button.tsx";
import { ArrowRight, FolderBookmark, UsersRound, Gauge } from "lucide-react";
import { Image } from "astro:assets";

import CharacterImage from "@/assets/pages/home/misana-character.png";
import BGGrid from "@/assets/pages/home/dark_bg-grid.svg";
import RTBracket from "@/assets/pages/home/quote-right_top-brackets.svg";
import LBBracket from "@/assets/pages/home/quote-left_bottom-brackets.svg";
---

<section
  id="sh-hero-section"
  class="hero-section relative h-svh overflow-hidden"
>
  <!-- 電腦 -->
  <div
    class="relative hidden h-full w-full items-center justify-center md:flex"
  >
    <!-- 背景互動元件 -->
    <div
      class="absolute top-45 -z-10 h-[200px] w-full bg-cover bg-center"
      aria-hidden="true"
    >
      <AnimateTitle />
    </div>

    <!-- 背景網格 -->
    <div
      class="absolute bottom-0 left-1/2 -z-20 -translate-x-1/2 opacity-60"
      aria-hidden="true"
    >
      <Image src={BGGrid} alt="背景網格" />
    </div>

    <!-- 立繪 -->
    <div class="absolute bottom-0 left-1/2 -translate-x-1/2">
      <img
        src={CharacterImage.src}
        alt="Hero Character"
        class="h-[80vh] w-auto max-w-none"
      />
    </div>

    <!-- 主框架 -->
    <div
      class="absolute bottom-30 left-1/2 mx-12 flex w-full max-w-[1246px] -translate-x-1/2 items-end justify-between overflow-hidden lg:mx-0 xl:items-start"
    >
      <!-- 左區段 -->
      <div class="flex max-w-110 flex-col gap-15">
        <h1 class="text-[96px]/[120%] font-black text-stone-200">
          Sam<br />Hacker
        </h1>

        <p class="text-lg text-stone-300">
          我是一個熱愛分享技術的高中生，<br />
          專注於 Minecraft 開服、網站建設、開源軟體等領域。<br />
          我的目標是讓更多人能夠輕鬆學習和應用技術。
        </p>

        <div class="flex gap-2.5">
          <a href="/about">
            <Button
              size="lg"
              variant="secondary"
              className="font-regular h-auto px-[30px]! py-2.5 text-lg"
            >
              關於我
              <ArrowRight className="ml-2.5" />
            </Button>
          </a>

          <a href="/projects">
            <Button size="icon-hero" variant="ghost" className="h-full p-0">
              <FolderBookmark size={24} className="size-5" />
            </Button>
          </a>

          <a href="/friends">
            <Button size="icon-hero" variant="ghost" className="h-full p-0">
              <UsersRound size={24} className="size-5" />
            </Button>
          </a>

          <a href="/dashboard">
            <Button size="icon-hero" variant="ghost" className="h-full p-0">
              <Gauge size={24} className="size-5" />
            </Button>
          </a>
        </div>
      </div>

      <!-- 右區段 -->
      <div
        class="relative mt-10 hidden max-h-[150px] min-h-[150px] w-full max-w-100 items-center justify-center lg:flex"
      >
        <Image
          src={RTBracket}
          alt="右上括號"
          class="absolute top-0 right-0 opacity-50"
        />
        <Image
          src={LBBracket}
          alt="左下括號"
          class="absolute bottom-0 left-0 opacity-50"
        />

        <p class="text-center font-funny text-3xl text-stone-300">
          正因為曾經淋過雨，<br />
          才會想為他人撐一把傘。
        </p>
      </div>
    </div>
  </div>

  <!-- 手機 -->
  <div
    class="relative -z-10 flex h-full w-full items-center justify-center md:hidden"
  >
    <div
      class="absolute top-1/2 left-1/2 h-[120px] w-full -translate-x-1/2 -translate-y-1/2 overflow-hidden"
      aria-hidden="true"
    >
      <AnimateTitle />
    </div>
  </div>

  <script>
    // Hero Section GSAP Animation
    import gsap from "gsap";
    import SplitText from "gsap/SplitText";

    gsap.registerPlugin(SplitText);

    document.addEventListener("DOMContentLoaded", () => {});
  </script>
</section>
